---
const { title } = Astro.props;
import { Image } from 'astro:assets';
import logoCOBAK from '../assets/cbphone-logo.png';
import favIcon from '../assets/favicon.ico';

---

<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content="Oficinas Virtuales en Mexicali - COBAK. Promoción especial: Paga 5 meses y obtén el 6to GRATIS. Servicios profesionales de dirección comercial, atención telefónica y más." />
    <link rel="icon" type="image/svg+xml" href={favIcon} />
    <!-- <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet"> -->
    <title>{title}</title>
  </head>
  <body>
    <div  class="fixed top-0 left-0 p-10 z-50 md:bg-white sm:bg-red-700">
      <Image src={logoCOBAK}  width="180" height="50" alt="COBAK page logo" />       
    </div>
    <slot />
  </body>
</html>

<style is:global>
  :root {
    --primary: #A31C15;
    --secondary: #686868;
    --terceary:#e47974;
  }

  body {
    font-family: 'Inter', sans-serif;
  }
  .logo-image {
    width: 180px;
    height: auto;
  }
</style>